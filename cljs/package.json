{
  "name": "app-manimani-cljs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "amu compile --target wasm32-browser app",
    "release": "rm -rf public/js && mkdir -p public/js && amu compile --target wasm32-browser app",
    "test": "amu compile --target wasm32-browser test && node out/tests.js",
    "watch": "amu compile --target wasm32-browser app"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.20"
  }
}
