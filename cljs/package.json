{
  "name": "app-manimani-cljs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "shadow-cljs compile app",
    "release": "rm -rf public/js && mkdir -p public/js && shadow-cljs release app",
    "test": "shadow-cljs compile test && node out/tests.js",
    "watch": "shadow-cljs watch app"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.20"
  }
}
